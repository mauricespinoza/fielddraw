"""Núcleo de FieldDraw Tiles.

`grid`, `pmtiles`, `mbtiles` y `pyramid` no importan QGIS, así que se pueden
probar con Python a secas (ver `qgis-plugin/tests`). `encoder`, `render` y
`tiler` sí necesitan GDAL —y `render`, opcionalmente, QGIS.
"""
